# Plan F — UI (Projekte + Workspaces) Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vollständige UI für Phase-2-Backend: Projekte-Seite mit Gedächtnis-Zähler + Memory-Tab, Workspaces-Liste statt redirect.

**Architecture:** Zwei unabhängige UI-Verbesserungen. Projects page: API liefert memory count mit → Badge + ausklappbarer Memory-Tab. Workspaces page: neues Server Component statt redirect → listet alle Workspaces des Users.

**Tech Stack:** Next.js App Router, React, Phosphor Icons, supabaseAdmin, CSS-Klassen aus globals.css

---

## File Map

| File | Action | Responsibility |
|------|--------|----------------|
| `src/app/api/projects/route.ts` | Modify | GET-Query: `project_memory(count)` ergänzen |
| `src/app/projects/page.tsx` | Modify | Memory-Count-Badge auf Karten + Memory-Tab im Edit-Panel |
| `src/app/workspaces/page.tsx` | Rewrite | Workspace-Liste (Server Component) statt redirect |

---

## Task 1: API — Memory Count im Projects List

**Files:**
- Modify: `src/app/api/projects/route.ts`

- [ ] **Step 1: Ergänze `project_memory(count)` in der SELECT-Abfrage**

In `GET /api/projects` die select-Zeile ändern:

```typescript
// Vorher:
.select('id, department_id, title, goal, instructions, meta, created_by, created_at, updated_at', { count: 'exact' })

// Nachher:
.select('id, department_id, title, goal, instructions, meta, created_by, created_at, updated_at, project_memory(count)', { count: 'exact' })
```

- [ ] **Step 2: TypeScript check**

```bash
cd "/c/Users/timmr/tropen OS" && pnpm exec tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/projects/route.ts
git commit -m "feat(projects/api): include project_memory count in list response"
```

---

## Task 2: Projects Page — Memory Badge + Memory Tab

**Files:**
- Modify: `src/app/projects/page.tsx`

- [ ] **Step 1: Typ erweitern**

`Project` Typ muss `project_memory` enthalten. In der Import-Sektion oder inline:

```typescript
// Ersetze den vorhandenen Project-Import aus useWorkspaceState durch einen lokalen Typ:
type ProjectWithMemory = {
  id:             string
  title:          string
  goal:           string | null
  instructions:   string | null
  department_id:  string
  created_at:     string
  project_memory: { count: number }[] | null
}
```

- [ ] **Step 2: Memory-Count-Badge auf den Karten**

Im Grid-Render jeder Projektkarte eine kleine Memory-Badge hinzufügen:

```tsx
{projects.map(p => {
  const memCount = p.project_memory?.[0]?.count ?? 0
  return (
    <button key={p.id} className="card"
      style={selected?.id === p.id ? s.cardItemActive : s.cardItem}
      onClick={() => selectProject(p)}
    >
      <p style={s.itemTitle}>{p.title}</p>
      {p.goal && <p style={s.itemSub}>{p.goal}</p>}
      {memCount > 0 && (
        <span style={{
          marginTop: 8, display: 'inline-flex', alignItems: 'center', gap: 4,
          fontSize: 11, color: 'var(--text-tertiary)',
        }}>
          <Brain size={12} weight="fill" aria-hidden="true" />
          {memCount} {memCount === 1 ? 'Erinnerung' : 'Erinnerungen'}
        </span>
      )}
    </button>
  )
})}
```

Import ergänzen: `Brain` von `@phosphor-icons/react`

- [ ] **Step 3: Memory Tab im Edit-Panel**

Neuen State `activeTab: 'edit' | 'memory'` hinzufügen. Im Edit-Panel (wenn `selected` gesetzt):

```tsx
{/* Tab-Navigation */}
<div style={{ display: 'flex', gap: 6, marginBottom: 16, borderBottom: '1px solid var(--border)', paddingBottom: 8 }}>
  <button
    className={`chip ${activeTab === 'edit' ? 'chip--active' : ''}`}
    onClick={() => setActiveTab('edit')}
  >Bearbeiten</button>
  <button
    className={`chip ${activeTab === 'memory' ? 'chip--active' : ''}`}
    onClick={() => { setActiveTab('memory'); loadMemories(selected.id) }}
  >Gedächtnis</button>
</div>

{activeTab === 'edit' && (
  /* vorhandenes Edit-Formular hier */
)}

{activeTab === 'memory' && (
  <div>
    {memoriesLoading ? (
      <p style={s.empty}>Lade Erinnerungen…</p>
    ) : memories.length === 0 ? (
      <p style={s.empty}>Noch keine Erinnerungen gespeichert.</p>
    ) : (
      <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        {memories.map((m, i) => (
          <div key={i} className="card" style={{ padding: '10px 12px' }}>
            <span style={{
              fontSize: 11, fontWeight: 600, textTransform: 'uppercase',
              color: 'var(--accent)', letterSpacing: '0.05em',
            }}>{m.type}</span>
            <p style={{ margin: '4px 0 0', fontSize: 13, color: 'var(--text-primary)', lineHeight: 1.5 }}>
              {m.content}
            </p>
            <p style={{ margin: '4px 0 0', fontSize: 11, color: 'var(--text-tertiary)' }}>
              {new Date(m.created_at).toLocaleDateString('de-DE', { day: '2-digit', month: '2-digit', year: 'numeric' })}
            </p>
          </div>
        ))}
      </div>
    )}
  </div>
)}
```

States und Load-Funktion:

```typescript
const [activeTab, setActiveTab] = useState<'edit' | 'memory'>('edit')
const [memories, setMemories] = useState<{ type: string; content: string; created_at: string }[]>([])
const [memoriesLoading, setMemoriesLoading] = useState(false)

async function loadMemories(projectId: string) {
  setMemoriesLoading(true)
  try {
    const res = await fetch(`/api/projects/${projectId}/memory`)
    if (res.ok) {
      const json = await res.json()
      setMemories(Array.isArray(json.data) ? json.data : [])
    }
  } finally {
    setMemoriesLoading(false)
  }
}
```

Tab beim Projektwechsel zurücksetzen:

```typescript
function selectProject(p: ProjectWithMemory) {
  setSelected(p)
  setActiveTab('edit')  // reset tab on project change
  setMemories([])
  // ... rest
}
```

- [ ] **Step 4: TypeScript check**

```bash
cd "/c/Users/timmr/tropen OS" && pnpm exec tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 5: Verify the memory API exists**

```bash
ls "/c/Users/timmr/tropen OS/src/app/api/projects/[id]/memory/"
```

Falls nicht vorhanden → create `src/app/api/projects/[id]/memory/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { supabaseAdmin } from '@/lib/supabase-admin'
import { getAuthUser, verifyProjectAccess } from '@/lib/api/projects'

export async function GET(
  _req: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const me = await getAuthUser()
  if (!me) return NextResponse.json({ error: 'Nicht autorisiert' }, { status: 401 })

  const { id } = await params
  const allowed = await verifyProjectAccess(id, me)
  if (!allowed) return NextResponse.json({ error: 'Nicht gefunden' }, { status: 404 })

  const { data, error } = await supabaseAdmin
    .from('project_memory')
    .select('type, content, created_at')
    .eq('project_id', id)
    .order('created_at', { ascending: false })
    .limit(50)

  if (error) return NextResponse.json({ error: error.message }, { status: 500 })
  return NextResponse.json({ data: data ?? [] })
}
```

- [ ] **Step 6: Commit**

```bash
git add src/app/projects/page.tsx src/app/api/projects/[id]/memory/route.ts
git commit -m "feat(projects/ui): add memory count badge + memory tab with history"
```

---

## Task 3: Workspaces List Page

**Files:**
- Rewrite: `src/app/workspaces/page.tsx`

- [ ] **Step 1: Schreibe die neue Workspaces-Liste als Server Component**

```tsx
// src/app/workspaces/page.tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/utils/supabase/server'
import { supabaseAdmin } from '@/lib/supabase-admin'
import WorkspacesList from '@/components/workspaces/WorkspacesList'

export default async function WorkspacesPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  // Load user's workspaces via participant memberships
  const { data: participantRows } = await supabaseAdmin
    .from('workspace_participants')
    .select('workspace_id')
    .eq('user_id', user.id)

  const ids = (participantRows ?? []).map((r: { workspace_id: string }) => r.workspace_id)

  let workspaces: {
    id: string
    title: string
    goal: string | null
    status: string
    created_at: string
    cards: { count: number }[]
  }[] = []

  if (ids.length > 0) {
    const { data } = await supabaseAdmin
      .from('workspaces')
      .select('id, title, goal, status, created_at, cards(count)')
      .in('id', ids)
      .is('deleted_at', null)
      .order('created_at', { ascending: false })

    workspaces = (data ?? []) as typeof workspaces
  }

  return <WorkspacesList workspaces={workspaces} />
}
```

- [ ] **Step 2: Schreibe die WorkspacesList Client Component**

```tsx
// src/components/workspaces/WorkspacesList.tsx
'use client'

import React, { useState } from 'react'
import { useRouter } from 'next/navigation'
import { SquaresFour, Plus, ChartBar } from '@phosphor-icons/react'

type Workspace = {
  id:         string
  title:      string
  goal:       string | null
  status:     string
  created_at: string
  cards:      { count: number }[]
}

const STATUS_LABEL: Record<string, string> = {
  draft:    'Entwurf',
  active:   'Aktiv',
  archived: 'Archiviert',
}

const STATUS_COLOR: Record<string, string> = {
  draft:    'var(--text-tertiary)',
  active:   'var(--accent)',
  archived: 'var(--text-tertiary)',
}

export default function WorkspacesList({ workspaces }: { workspaces: Workspace[] }) {
  const router = useRouter()
  const [creating, setCreating] = useState(false)
  const [newTitle, setNewTitle] = useState('')
  const [saving, setSaving] = useState(false)

  async function handleCreate() {
    if (!newTitle.trim()) return
    setSaving(true)
    try {
      const res = await fetch('/api/workspaces', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ title: newTitle.trim() }),
      })
      if (res.ok) {
        const ws = await res.json()
        router.push(`/workspaces/${ws.id}`)
      }
    } finally {
      setSaving(false)
    }
  }

  return (
    <div className="content-max">
      <div className="page-header">
        <div className="page-header-text">
          <h1 className="page-header-title">
            <SquaresFour size={22} color="var(--text-primary)" weight="fill" aria-hidden="true" />
            Workspaces
          </h1>
          <p className="page-header-sub">Deine KI-Arbeitsbereiche mit Karten und Outcomes</p>
        </div>
        <div className="page-header-actions">
          <button className="btn btn-primary" onClick={() => setCreating(true)}>
            <Plus size={14} weight="bold" /> Neuer Workspace
          </button>
        </div>
      </div>

      {creating && (
        <div className="card" style={{ padding: 14, marginBottom: 16 }}>
          <input
            autoFocus
            placeholder="Workspace-Name"
            value={newTitle}
            onChange={e => setNewTitle(e.target.value)}
            onKeyDown={e => {
              if (e.key === 'Enter') handleCreate()
              if (e.key === 'Escape') { setCreating(false); setNewTitle('') }
            }}
            style={{
              width: '100%', padding: '8px 10px', marginBottom: 10,
              border: '1px solid var(--border)', borderRadius: 6,
              background: 'var(--bg-surface)', color: 'var(--text-primary)',
              fontSize: 14, outline: 'none',
            }}
          />
          <div style={{ display: 'flex', gap: 6 }}>
            <button className="btn btn-primary btn-sm" onClick={handleCreate} disabled={saving || !newTitle.trim()}>
              Erstellen
            </button>
            <button className="btn btn-ghost btn-sm" onClick={() => { setCreating(false); setNewTitle('') }}>
              Abbrechen
            </button>
          </div>
        </div>
      )}

      {workspaces.length === 0 && !creating ? (
        <p style={{ color: 'var(--text-tertiary)', fontSize: 14 }}>
          Noch keine Workspaces — erstelle deinen ersten.
        </p>
      ) : (
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))', gap: 12 }}>
          {workspaces.map(ws => {
            const cardCount = ws.cards?.[0]?.count ?? 0
            return (
              <button
                key={ws.id}
                className="card"
                style={{ padding: '16px 18px', textAlign: 'left', cursor: 'pointer' }}
                onClick={() => router.push(`/workspaces/${ws.id}`)}
              >
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 6 }}>
                  <p style={{ margin: 0, fontSize: 14, fontWeight: 600, color: 'var(--text-primary)' }}>
                    {ws.title}
                  </p>
                  <span style={{ fontSize: 11, color: STATUS_COLOR[ws.status] ?? 'var(--text-tertiary)', flexShrink: 0, marginLeft: 8 }}>
                    {STATUS_LABEL[ws.status] ?? ws.status}
                  </span>
                </div>
                {ws.goal && (
                  <p style={{ margin: '0 0 10px', fontSize: 12, color: 'var(--text-secondary)', lineHeight: 1.4 }}>
                    {ws.goal}
                  </p>
                )}
                <div style={{ display: 'flex', alignItems: 'center', gap: 4, marginTop: 8 }}>
                  <ChartBar size={12} weight="fill" color="var(--text-tertiary)" aria-hidden="true" />
                  <span style={{ fontSize: 11, color: 'var(--text-tertiary)' }}>
                    {cardCount} {cardCount === 1 ? 'Karte' : 'Karten'}
                  </span>
                </div>
              </button>
            )
          })}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 3: TypeScript check**

```bash
cd "/c/Users/timmr/tropen OS" && pnpm exec tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 4: Commit**

```bash
git add src/app/workspaces/page.tsx src/components/workspaces/WorkspacesList.tsx
git commit -m "feat(workspaces/ui): replace redirect with workspace list + card count"
```

---

## Task 4: Update CLAUDE.md + phase2-plans.md

- [ ] **Step 1: Update phase2-plans.md** — Plan F Status auf ✅
- [ ] **Step 2: Update CLAUDE.md** — Plan F Sektion hinzufügen
- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md docs/phase2-plans.md
git commit -m "chore: update docs — Plan F done"
```
